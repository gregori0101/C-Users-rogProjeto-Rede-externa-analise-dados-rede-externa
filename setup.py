from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='analise de dados em tempo real de indicadores e lista de falhas na rede para serem tratadas',
    author='Gregori',
    license='',
)
